# PostgreSQL Style Guide Topic Map for AI Coding Agents

## Framing the project

Your target is well chosen: PostgreSQL 18 is the current supported major version, and PostgreSQL 18.4 is the current minor release as of July 2, 2026; 18.4 was released on May 14, 2026. PostgreSQL 18 also introduced a few features that can materially affect style-guide decisions, including `uuidv7()`, virtual generated columns as the default generated-column kind, and temporal constraints over ranges for `PRIMARY KEY`, `UNIQUE`, and `FOREIGN KEY` constraints. Those are exactly the sorts of changes that make version-pinned SQL guidance worthwhile for AI agents. citeturn0search1turn0search2turn0search5turn7search2turn19search5

For a skill that an agent will consult repeatedly, I would recommend **24 core markdown pages**. That is small enough to stay maintainable, but large enough to separate “orthogonal” decisions that agents can apply independently when writing schemas and queries. The most important design principle is not completeness; it is **decision clarity**. Anywhere PostgreSQL supports multiple valid patterns, the page should force a house rule or a rule-with-exceptions, so the agent does not improvise between competing styles. That is especially important for identifiers, key strategy, JSON/array usage, CTE usage, indexing, function security, and permissions. citeturn10view3turn11view0turn9view0turn8view3turn12view2

One optional overflow topic is worth keeping in reserve if you later decide you want a 25th page: **collation and locale policy**. PostgreSQL’s collation behavior can affect equality semantics, indexing assumptions, and case-insensitive behavior; PostgreSQL 18 also expanded support around nondeterministic collations. If your agents will write text-search or case-insensitive matching logic in multiple locales, that page may deserve promotion into the core set. citeturn13search1turn13search4turn13search10turn13search13

## Foundations and naming

The first cluster should remove ambiguity from generated SQL. PostgreSQL’s identifier folding, keyword rules, and schema resolution behavior create many “technically valid but operationally painful” cases unless you set explicit conventions. The foundation pages should be strict and compact, because these are the rules your agents will reuse in nearly every file they generate. citeturn10view3turn17view0turn9view2

| Topic page | Why it deserves its own page | Decision points the skill must settle |
|---|---|---|
| **Identifier casing and quoting** | PostgreSQL folds unquoted identifiers to lower case; quoted identifiers become case-sensitive, and keyword collisions can require quoting or `AS`. A house rule here prevents a lot of brittle generated SQL. citeturn10view3turn17view0 | Ban quoted identifiers entirely except for legacy interoperability, or permit them selectively? Require `snake_case` lower case only? Are mixed-case legacy names ever acceptable in new DDL? |
| **Object naming across tables, columns, constraints, indexes, functions, triggers, and views** | PostgreSQL allows custom names for constraints, and index-backed constraints share index naming constraints within a schema. Naming needs to be systematic if AI-generated migrations are going to stay predictable. citeturn15view0turn8view1 | Singular vs plural table names. Stable suffixes like `_pkey`, `_fkey`, `_idx`, `_uniq`, `_check`, `_excl`, `_trg`, `_fn`, `_mv`. Whether names should encode table name, column list, and predicate intent. |
| **Schema layout and namespace policy** | Schemas organize objects, but they also affect security through `search_path`. PostgreSQL’s docs explicitly discuss secure schema usage and the dangers of writable schemas on the path. citeturn9view2turn4search6turn4search14 | Single application schema vs multiple domain schemas. Whether agents should always schema-qualify cross-schema references. Whether to forbid implicit reliance on `search_path` in application SQL and migrations. |
| **SQL formatting, aliases, qualification, and comments** | PostgreSQL’s grammar and keyword set make some formatting choices more robust than others, especially around aliases, column labels, and reserved words. Good formatting is not purely aesthetic for agent-written SQL; it improves parseability, reviewability, and diff stability. citeturn17view0turn10view3turn5search0 | Uppercase vs lowercase keywords. Trailing vs leading commas. Required table aliases for every `FROM` item or only when needed. Mandatory qualification for all columns in joins. Inline comments vs block comments. Whether to forbid ambiguous shorthand such as implicit joins. |

## Schema and data modeling topics

This cluster should define the default “shape” of your data model. It is where agents need the clearest rules, because otherwise they will oscillate between different PostgreSQL idioms: integer identities versus UUIDs, highly normalized schemas versus `jsonb`, enums versus lookup tables, and generated columns versus application-maintained derivations. PostgreSQL supports all of these patterns; your skill should decide where each one belongs. citeturn10view0turn10view1turn10view2turn8view2turn12view1turn15view1turn15view3

| Topic page | Why it deserves its own page | Decision points the skill must settle |
|---|---|---|
| **Keys, identifiers, and row identity** | PostgreSQL supports primary keys, identity columns, native UUID storage, and in 18 also `uuidv7()` for time-ordered UUID generation. Identity columns do not themselves enforce uniqueness; that still comes from `PRIMARY KEY` or `UNIQUE`. citeturn10view0turn7search4turn10view2turn8view1 | Default PK type: `bigint GENERATED ... AS IDENTITY` or `uuid DEFAULT uuidv7()`? If UUIDs, version 7 or version 4? Natural keys ever allowed as PKs, or only as alternate unique constraints? |
| **Foreign-key relationship modeling** | PostgreSQL’s FK support is rich: single-column, composite, self-referential, default target to referenced PK, and deferrability controls. Relationship style needs its own page because schema clarity and cascade behavior are long-lived decisions. citeturn8view1turn1search9 | Require FKs by default, or allow “soft references” in some event/log tables? Which `ON DELETE` actions are preferred? Are self-referential trees modeled with FKs by default? |
| **Standard metadata and lifecycle columns** | PostgreSQL columns can use defaults, identity definitions, and generated columns; generated columns are virtual by default in PostgreSQL 18. This is the right place to standardize fields such as `created_at`, `updated_at`, `deleted_at`, tenant IDs, version counters, and derived display/search columns. citeturn10view0turn10view1turn3search9 | Which lifecycle columns are required on most tables? Should `updated_at` be trigger-maintained or application-written? Prefer stored generated columns, virtual generated columns, or ordinary columns for derivations? |
| **Scalar type selection** | PostgreSQL’s type system is broad, and type choices have real behavioral consequences. The docs explicitly separate exact numerics, floating point, UUID, date/time, JSON, arrays, ranges, and more. For style-guide purposes, a strong default map prevents drift. citeturn2search9turn15view2turn13search18 | `text` vs `varchar(n)` default. `numeric` vs integer cents for money-like values. Whether `money` is banned. Whether booleans should ever be nullable. Preferred integer widths (`integer` vs `bigint`). |
| **Temporal and time-zone conventions** | PostgreSQL supports the full SQL date/time family, and range types like `tsrange`/`tstzrange` are often a better semantic fit than separate start/end columns for some models. Time handling is a classic source of inconsistency in generated SQL. citeturn15view2turn15view3turn13search11 | Default to `timestamptz` everywhere, or only for user-facing/event times? Are `date` and `timestamp without time zone` allowed in business schemas? Represent intervals as `interval`, integer seconds, or not at all except in analytical SQL? |
| **Semi-structured modeling with normalization, `jsonb`, and arrays** | PostgreSQL supports first-class `json`/`jsonb` and arrays, and `jsonb` has distinct operator-class tradeoffs in indexing. This is one of the biggest “multiple right answers” areas in Postgres design. citeturn8view2turn12view0turn2search4turn2search8 | Default to normalized tables unless there is a clear reason not to, or actively encourage `jsonb` for fast-moving attributes? When are arrays allowed? For `jsonb`, when must keys be documented and indexed? |
| **Enums, domains, ranges, and other reusable types** | PostgreSQL enums are static and ordered, domains centralize reusable constraints, and ranges/multiranges model intervals directly. These are powerful tools, but they also create migration and interoperability consequences, so they need explicit policy. citeturn12view1turn15view1turn15view3turn2search14 | Prefer enum types, lookup tables, or `text` plus `CHECK` for status fields? When should a domain replace repeated column-level constraints? When should date/time periods use range types instead of start/end columns? |

## Constraints and access paths

This cluster is where correctness and performance meet. PostgreSQL’s docs are very clear that indexes speed reads but add write overhead, that unique and primary-key constraints automatically create unique B-tree indexes, that exclusion constraints are the right fit for non-overlap rules, and that advanced index forms exist for partial, computed, and semi-structured access patterns. These are perfect candidates for one-page rules because agent-written DDL often gets these half-right. citeturn10view4turn8view1turn15view4turn1search18turn1search2turn1search6

| Topic page | Why it deserves its own page | Decision points the skill must settle |
|---|---|---|
| **Constraints and null semantics** | PostgreSQL distinguishes `NOT NULL`, `CHECK`, `UNIQUE`, `PRIMARY KEY`, `FOREIGN KEY`, `EXCLUDE`, and deferrability. Unique constraints can also opt into `NULLS NOT DISTINCT`, which is a major semantic choice. PostgreSQL 18 also adds temporal constraints over ranges. citeturn8view1turn14search2turn14search1 | Default to `NOT NULL` unless absence is semantically meaningful? Should uniqueness with nullable columns usually use `NULLS NOT DISTINCT`? When are deferred FKs or deferred uniqueness allowed? When should non-overlap constraints use `EXCLUDE` rather than application logic? |
| **Index basics** | PostgreSQL documents that indexes improve retrieval speed but add overhead, and that PK/unique constraints create unique B-tree indexes automatically. A default indexing doctrine keeps agents from both under-indexing and cargo-cult over-indexing. citeturn10view4turn8view1turn2search18 | Rule for indexing every FK or not. Rule for indexing every unique business key. Whether agents should add indexes only when query patterns justify them. Naming convention and column-order convention for ordinary B-tree indexes. |
| **Advanced indexes** | PostgreSQL supports multicolumn, partial, expression, GIN, and operator-class-specific indexes; for `jsonb`, `jsonb_ops` and `jsonb_path_ops` have different tradeoffs. Advanced index usage should not be left to improvisation. citeturn1search6turn1search2turn1search18turn8view2turn2search8 | When to prefer partial indexes over full-table indexes. When expression indexes are acceptable. For `jsonb`, default operator class for containment-heavy workloads. Column ordering rules for multicolumn indexes. |
| **Partitioning policy for large tables** | Declarative partitioning and legacy inheritance have different capabilities; declarative partitioning is suitable for most common use cases, but introduces its own constraint/index tradeoffs. citeturn9view4turn6search3turn14search13 | Threshold for partitioning by table size, write rate, or retention policy. Preferred strategy: range, list, or hash. Whether agents should ever introduce inheritance-based partitioning, or whether that is banned outside specialist cases. |

## Query-writing topics

This cluster should codify how agents write everyday SQL. PostgreSQL exposes real semantic differences between join forms, subquery forms, CTE behavior, and pagination or aggregation patterns. If you do not write these rules down, an agent will often generate something valid but inconsistent with your preferences and, in some cases, subtly wrong. citeturn5search0turn16view1turn16view0turn11view0turn5search7turn19search2

| Topic page | Why it deserves its own page | Decision points the skill must settle |
|---|---|---|
| **`SELECT` structure and join style** | PostgreSQL distinguishes inner, left, right, full, cross, and lateral joins, and join filters behave differently from post-join filters. A join-style page is the backbone of query readability. citeturn5search0turn5search4turn18view0 | Require explicit `JOIN ... ON` instead of comma joins. Ban `NATURAL JOIN`. Prefer `ON` over `USING`. Require all selected columns to be qualified in multi-table queries. Require left joins over correlated subqueries in some cases, or not. |
| **Subqueries, `EXISTS`, `IN`, `NOT IN`, and `LATERAL`** | PostgreSQL docs explicitly call out `EXISTS` conventions, `NOT IN` null traps, and `LATERAL` semantics. This is fertile ground for agent mistakes and should be normalized. citeturn16view1turn16view0turn16view2 | Prefer `EXISTS (SELECT 1 ...)` for semi-joins? Ban `NOT IN` against nullable subqueries in favor of `NOT EXISTS`? When should row constructors or `ANY`/`ALL` be used? When is `LATERAL` encouraged for clarity? |
| **CTEs and query decomposition** | PostgreSQL’s `WITH` queries are not just a formatting choice; evaluation and materialization can affect performance, and `MATERIALIZED` / `NOT MATERIALIZED` can override planner behavior. citeturn8view0turn11view0turn11view3 | This is one of your explicit preference pages. Should agents introduce CTEs once a query passes a complexity threshold? Should they default to readability-first CTEs, and how should they handle performance-sensitive cases where `NOT MATERIALIZED` matters? |
| **Aggregation, window functions, ordering, and pagination** | PostgreSQL’s window functions preserve row identity while adding analytic calculations, and ordering/pagination choices affect determinism and reviewability. This page governs query shape after the `FROM`/`WHERE` core. citeturn5search3turn5search7turn18view0 | Prefer window functions over self-joins for ranking and running totals? Require explicit `ORDER BY` whenever `LIMIT` is present? Offset pagination vs keyset pagination for application-facing queries? |
| **DML patterns with `INSERT`, `UPDATE`, `DELETE`, `MERGE`, `RETURNING`, and upserts** | PostgreSQL supports `RETURNING` on all major DML commands, `MERGE` for conditional multi-action writes, and `ON CONFLICT` for deterministic upserts, but each carries style implications. PostgreSQL 18 also added `OLD`/`NEW` support in `RETURNING`. citeturn19search2turn19search1turn14search7turn19search5turn19search8 | Prefer `INSERT ... ON CONFLICT` or `MERGE` when both could work? Require `RETURNING` instead of follow-up reads after writes? Should bulk changes prefer data-modifying CTEs, temporary tables, or multiple statements? |

## Programmable database topics

These pages should cover the SQL objects that live longer than a single query. PostgreSQL gives functions, views, materialized views, and triggers real optimizer and security behavior; they are not just packaging conveniences. This is also where it becomes especially important to stop agents from generating “smart” but surprising database logic. citeturn9view0turn8view3turn6search1turn6search6

| Topic page | Why it deserves its own page | Decision points the skill must settle |
|---|---|---|
| **Functions and procedures** | PostgreSQL functions have volatility categories, null-input behavior, security modes, and parallel-safety labels; the docs explicitly say functions should be labeled with the strictest valid volatility category. citeturn9view0turn9view1 | Prefer plain SQL functions over PL/pgSQL when possible? Default to `SECURITY INVOKER` only? When are `IMMUTABLE`, `STABLE`, and `VOLATILE` each acceptable? Should agents avoid procedures unless transaction control is truly needed? |
| **Views and materialized views** | PostgreSQL views have privilege behavior, optional `security_invoker`, updatability rules, and materialized views have refresh semantics and permission requirements. The docs even call out style concerns like explicitly naming literal-derived columns. citeturn8view3turn6search1turn6search5turn6search9 | Use ordinary views for read APIs, or prefer application-side query composition? When is a materialized view justified? Should views always alias every output column explicitly? Should `security_invoker` ever be the default? |
| **Triggers** | PostgreSQL triggers can be row-level or statement-level, request transition relations, and alter `NEW`/`OLD` semantics in ways that affect `RETURNING`, `ON CONFLICT`, and auditing. Trigger usage needs a tight policy. citeturn8view4turn6search6turn6search10turn19search15 | Minimal-trigger doctrine or trigger-friendly doctrine? Restrict triggers to auditing and invariant enforcement, or permit business logic? Prefer generated columns or application writes over update triggers where possible? |

## Security and execution boundaries

A style guide for agent-written PostgreSQL should absolutely include a page on security and privileges, because PostgreSQL’s permission model is object-based, role-based, and affected by schema lookup behavior. PostgreSQL roles can represent both users and groups; grants have per-object and role-membership variants; views and `SECURITY DEFINER` functions can execute with privileges different from the caller; row security policies introduce another layer of access control; and the docs repeatedly warn about unsafe `search_path` usage. citeturn12view2turn12view3turn8view3turn9view0turn9view3turn9view2turn12view4

| Topic page | Why it deserves its own page | Decision points the skill must settle |
|---|---|---|
| **Roles, grants, row-level security, and secure execution** | PostgreSQL unifies users and groups into roles, supports role inheritance and `SET ROLE`, offers predefined roles, applies policies via RLS, and warns that `search_path` and `SECURITY DEFINER` must be handled carefully. citeturn12view2turn12view3turn9view3turn9view0turn12view4turn9view2 | Application role model: owner role, migration role, app runtime role, read-only/reporting role, admin role. Default `GRANT` posture: deny by default or broader shared access. When to use RLS versus separate schemas/databases. Whether every security-sensitive object must schema-qualify references and set `search_path` defensively. |

## Decisions that must be made before drafting

The skill can only work well if you settle a handful of contentious choices up front. These are the places where PostgreSQL supports multiple good answers, but an AI agent needs **one preferred answer** or a very small set of exception rules.

| Decision area | Why this is a real fork in the road | Recommendation for your eventual house rule |
|---|---|---|
| **Quoted identifiers** | PostgreSQL lower-cases unquoted identifiers and makes quoted ones case-sensitive. Supporting both styles makes generated SQL much harder to keep consistent. citeturn10view3 | Favor a near-total ban on quoted identifiers in new code. |
| **Primary key strategy** | PostgreSQL 18 supports both identity columns and native UUID generation, including `uuidv7()` for time-ordered UUIDs. citeturn10view0turn10view2turn7search2 | Pick one default. If you want globally unique IDs with better locality than v4, `uuidv7()` is a strong modern default; if you want simpler joins/storage, `bigint` identity is still excellent. |
| **Normalization versus `jsonb` and arrays** | PostgreSQL is unusually strong at semi-structured modeling, which means agents will reach for it unless constrained. `jsonb` indexing choices also change query behavior and storage tradeoffs. citeturn8view2turn12view0turn2search8 | Prefer normalized tables by default; permit `jsonb` for genuinely sparse, fast-evolving, or externally sourced attributes; use arrays more narrowly than `jsonb`. |
| **Enums versus lookup tables versus `text` plus `CHECK`** | Enum values are static and ordered, and can be extended later, but that evolution path is different from lookup tables or text validation. Domains solve a different reuse problem. citeturn12view1turn2search14turn15view1 | Use enums only for small, stable, low-churn state spaces. Use lookup tables for user-managed vocabularies. Use domains for repeated scalar validation. |
| **Null policy** | PostgreSQL uniqueness semantics with nulls are configurable, and `NOT IN` also has surprising null behavior. Silent null choices become silent bugs. citeturn8view1turn16view0 | Default to `NOT NULL` unless null has a clear domain meaning. Use `NULLS NOT DISTINCT` intentionally where null should still behave “as a value” for uniqueness. |
| **CTE decomposition threshold** | PostgreSQL CTEs are valuable for readability, but materialization behavior can help or hurt. Your personal preference already points toward a clarity-first policy for large queries. citeturn11view0turn11view3 | Make “decompose large queries with CTEs” the default. Add an explicit exception rule for hot queries where `NOT MATERIALIZED`, inline subqueries, or other rewrites are measurably better. |
| **Advanced indexing policy** | PostgreSQL provides many special-purpose indexes, and the “best” one depends on workload shape. Agents should not guess. citeturn1search2turn1search18turn1search6turn8view2 | Set default rules for when partial, expression, multicolumn, and GIN indexes are allowed, and require a stated query pattern for each. |
| **Function and trigger discipline** | PostgreSQL functions carry optimizer and security promises, and triggers can quietly centralize business logic. That power cuts both ways. citeturn9view0turn9view1turn6search6turn19search15 | Adopt a conservative rule: functions should be explicit about volatility and security; triggers should be rare, narrow, and never the default place for business workflow logic. |
| **View security model** | Views can operate with privilege behavior tied to the owner or invoker, and `security_barrier` matters in sensitive cases. citeturn8view3turn4search17 | Decide whether views are just a presentation abstraction or part of your security boundary. If the latter, write much stricter rules for `security_invoker`, `security_barrier`, and underlying function usage. |
| **Role and grant posture** | PostgreSQL supports direct object grants, role membership, predefined roles, and RLS. Without a fixed pattern, agents tend to generate ad hoc grants. citeturn12view2turn12view3turn12view4turn9view3 | Prefer a small standard role topology and deny-by-default grants. Make agents grant to roles, not directly to human users. |

## Recommended page set

If you want the shortest practical answer to “what should the skill contain,” my recommended **24-page core set** is this:

1. Identifier casing and quoting  
2. Object naming across all database objects  
3. Schema layout and namespace policy  
4. SQL formatting, aliases, qualification, and comments  
5. Keys, identifiers, and row identity  
6. Foreign-key relationship modeling  
7. Standard metadata and lifecycle columns  
8. Scalar type selection  
9. Temporal and time-zone conventions  
10. Semi-structured modeling with normalization, `jsonb`, and arrays  
11. Enums, domains, ranges, and reusable types  
12. Constraints and null semantics  
13. Index basics  
14. Advanced indexes  
15. Partitioning policy  
16. `SELECT` structure and join style  
17. Subqueries, `EXISTS`, `IN`, `NOT IN`, and `LATERAL`  
18. CTEs and query decomposition  
19. Aggregation, window functions, ordering, and pagination  
20. DML patterns with upserts and `RETURNING`  
21. Functions and procedures  
22. Views and materialized views  
23. Triggers  
24. Roles, grants, row-level security, and secure execution

That set maps cleanly to the PostgreSQL 18.4 feature surface, covers the areas you explicitly named, and isolates the high-variance choices that most often cause agents to produce inconsistent SQL. It also leaves you room for exactly one overflow page later if you decide that collations/locales, migration-safe DDL, or extension policy deserves first-class treatment. citeturn0search2turn7search2turn10view3turn10view0turn8view2turn11view0turn9view0turn8view3turn9view3